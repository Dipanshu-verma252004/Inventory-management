@extends('layouts.master')
@section('title', 'Low Stock Report')
@section('content')
<div class="card"><div class="card-header d-flex justify-content-between align-items-center"><h4 class="mb-0">Low Stock Report</h4><span class="badge bg-warning">{{ $products->total() }} item(s) need attention</span></div><div class="card-body"><div class="table-responsive"><table class="table table-bordered table-hover align-middle"><thead><tr><th>#</th><th>Product</th><th>Category</th><th>Brand</th><th>Current Stock</th><th>Minimum Stock</th><th>Shortage</th><th>Status</th></tr></thead><tbody>@forelse($products as $product)<tr><td>{{ $products->firstItem() + $loop->index }}</td><td>{{ $product->product_name }}</td><td>{{ $product->category?->category_name ?? '-' }}</td><td>{{ $product->brand?->brand_name ?? '-' }}</td><td>{{ $product->opening_stock }} {{ $product->unit?->unit_name }}</td><td>{{ $product->minimum_stock }}</td><td>{{ max(0, $product->minimum_stock - $product->opening_stock) }}</td><td><span class="badge {{ $product->opening_stock <= 0 ? 'bg-danger' : 'bg-warning' }}">{{ $product->opening_stock <= 0 ? 'Out of Stock' : 'Low Stock' }}</span></td></tr>@empty<tr><td colspan="8" class="text-center text-muted">All products are above their minimum stock level.</td></tr>@endforelse</tbody></table></div>{{ $products->links() }}</div></div>
@endsection
