@extends('layouts.master')
@section('title', 'Purchase Report')
@section('content')
<div class="card"><div class="card-header"><h4 class="mb-0">Purchase Report</h4></div><div class="card-body">
<form method="GET" class="row g-2 mb-4"><div class="col-md-4"><select name="supplier_id" class="form-select"><option value="">All Suppliers</option>@foreach($suppliers as $supplier)<option value="{{ $supplier->id }}" @selected(request('supplier_id') == $supplier->id)>{{ $supplier->supplier_name }}</option>@endforeach</select></div><div class="col-md-3"><input type="date" name="from_date" value="{{ request('from_date') }}" class="form-control"></div><div class="col-md-3"><input type="date" name="to_date" value="{{ request('to_date') }}" class="form-control"></div><div class="col-md-2 d-grid"><button class="btn btn-primary">Filter</button></div></form>
<div class="row mb-4"><div class="col-md-4"><div class="alert alert-primary mb-0">Total Purchase: <strong>₹ {{ number_format($totals->total, 2) }}</strong></div></div><div class="col-md-4"><div class="alert alert-success mb-0">Paid: <strong>₹ {{ number_format($totals->paid, 2) }}</strong></div></div><div class="col-md-4"><div class="alert alert-warning mb-0">Due: <strong>₹ {{ number_format($totals->due, 2) }}</strong></div></div></div>
<div class="table-responsive"><table class="table table-bordered table-hover"><thead><tr><th>#</th><th>Purchase No.</th><th>Date</th><th>Supplier</th><th class="text-end">Total</th><th class="text-end">Paid</th><th class="text-end">Due</th></tr></thead><tbody>@forelse($purchases as $purchase)<tr><td>{{ $purchases->firstItem() + $loop->index }}</td><td>{{ $purchase->purchase_no }}</td><td>{{ $purchase->purchase_date }}</td><td>{{ $purchase->supplier?->supplier_name ?? '-' }}</td><td class="text-end">₹ {{ number_format($purchase->total_amount, 2) }}</td><td class="text-end">₹ {{ number_format($purchase->paid_amount, 2) }}</td><td class="text-end">₹ {{ number_format($purchase->due_amount, 2) }}</td></tr>@empty<tr><td colspan="7" class="text-center text-muted">No purchases found.</td></tr>@endforelse</tbody></table></div>{{ $purchases->links() }}</div></div>
@endsection
